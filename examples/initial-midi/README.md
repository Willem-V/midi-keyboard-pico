# Initial MIDI Example

Early USB MIDI implementation template with basic GPIO testing.

## What it does
- Basic USB MIDI setup with TinyUSB
- Simple GPIO read (GPIO 16/0) with LED toggle
- USB MIDI task handling

## Building
```bash
cd examples/initial-midi
./build.sh
```

**Note**: The hardcoded SDK path in CMakeLists.txt may need updating.
